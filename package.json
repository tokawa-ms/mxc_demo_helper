{
  "name": "mxc-demo-helper",
  "version": "0.1.0",
  "private": true,
  "description": "Localhost browser dashboard for the MXC Windows policy demo scripts.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}